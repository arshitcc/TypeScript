const nums1 = [];
// nums1.push(0); // cant

const nums2 : [] = []; 
// nums2.push(0); // cant

const nums3 : number[] = [];
// nums3.push(0); // now-yes

const nums: number[] = [];
nums.push(1);

const superHeroes : Array<String> = [];
superHeroes.push("IronMan")


type User = {
    name : string,
    email : string,
    age : number,
    isActive : boolean
}

const allUsers: User[] = [];
allUsers.push({name :'Arshit', email : 'xx@xx.com', age : 0, isActive : true});

const MLModels: number[][] = [
    [1,2,3],
    [4,5,6],
]

export {}



